<ui:FluentWindow x:Class="MuSync.SteamGuardCodeWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 Title="Steam Guard 验证" Width="360" Height="220"
                 ResizeMode="NoResize" ShowInTaskbar="False"
                 WindowStartupLocation="CenterOwner"
                 ExtendsContentIntoTitleBar="True" WindowBackdropType="Mica">
    <Grid Margin="24,32,24,18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="PromptText" TextWrapping="Wrap" FontSize="13"
                   Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
        <ui:TextBox x:Name="CodeBox" Grid.Row="1" Margin="0,12,0,0" PlaceholderText="5 位验证码" />

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ui:Button x:Name="CancelButton" Content="取消" IsCancel="True" Width="88" Margin="0,0,10,0" />
            <ui:Button x:Name="ConfirmButton" Content="确定" Appearance="Primary" IsDefault="True"
                       Width="88" Grid.Column="1" Click="ConfirmButton_Click" />
        </Grid>
    </Grid>
</ui:FluentWindow>
