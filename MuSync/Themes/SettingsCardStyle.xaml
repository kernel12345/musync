<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">
    <!--
      Windows 11 设置风格卡片：WPF-UI 4.x 无内置 SettingsCard，用 Border+Grid 自定义。
      全部使用 WPF-UI 主题画刷（DynamicResource），深浅色切换时自动跟随。
    -->
    <!-- 通用圆角卡片（设置页 SettingsCard 同款：一行图标+文本+右侧操作） -->
    <Style x:Key="SettingsCard" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource ControlFillColorDefaultBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource ControlElevationBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="6" />
        <Setter Property="Padding" Value="16,12" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>
    <!-- 主界面播放器大卡片（更高、可承载封面+进度条） -->
    <Style x:Key="PlayerCard" TargetType="Border" BasedOn="{StaticResource SettingsCard}">
        <Setter Property="Padding" Value="16,14" />
        <Setter Property="Margin" Value="0,0,0,10" />
    </Style>
    <!-- 设置页节标题 -->
    <Style x:Key="SettingsSectionHeader" TargetType="TextBlock">
        <Setter Property="FontSize" Value="20" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
        <Setter Property="Margin" Value="0,16,0,8" />
    </Style>
    <!-- 卡片标题行 -->
    <Style x:Key="SettingsCardTitle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
    </Style>
    <!-- 卡片描述行 -->
    <Style x:Key="SettingsCardDescription" TargetType="TextBlock">
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}" />
        <Setter Property="TextWrapping" Value="Wrap" />
    </Style>
    <!-- 卡片图标（默认 20px，次级色） -->
    <Style x:Key="SettingsCardIcon" TargetType="ui:SymbolIcon">
        <Setter Property="FontSize" Value="20" />
        <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}" />
    </Style>
</ResourceDictionary>
