# Changelog

本项目的版本变更记录。格式参照 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，
版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [0.2.5] - 2026-09-17

### 修复

- 修复点击侧边栏「桌面」页**卡死并闪退**的问题：透明度卡片图标误用了 WPF-UI 4.3.0 中不存在的
  `SymbolRegular.Opacity24`（XAML 枚举名在编译期不校验，运行时加载页面才抛出 `XamlParseException`），
  改为有效的 `ColorBackground24`。

## [0.2.4] - 2026-09-17

### 新增

- **侧边栏新增「桌面」页面**，桌面相关设置从「设置」页迁移至此：
  - 双击桌面空白处隐藏图标开关（约 400ms 渐隐/渐显，再次双击恢复）。
  - **桌面图标透明度调节**：滑块 20%-100% 实时预览常驻不透明度，退出程序自动恢复系统默认；
    透明度与渐隐动画协同，隐藏后再恢复仍停留在设定值。

### 修复

- 修复在桌面图标上**双击打开应用时也会触发渐隐**的问题：图标命中检测改为“确认在空白处才隐藏”，
  任一枚举/跨进程读取失败一律按图标区域放行；图标包围矩形外扩 8px 容差，双击判定区域按鼠标所在
  显示器 DPI 缩放，避免高缩放比例下边缘误判。

## [0.2.3] - 2026-09-16

### 新增

- **双击桌面空白处隐藏图标**：在桌面空白处双击即可渐隐隐藏全部桌面图标，再次双击渐显恢复
  （设置 → 程序设置 → 「双击桌面隐藏图标」，默认关闭）。参照 [DeskHider](https://github.com/iandiv/DeskHider)
  方案实现：取鼠标下顶层窗口类名判定桌面，跨进程枚举图标包围矩形（`LVM_GETITEMRECT` +
  `VirtualAllocEx`）判定空白处——双击图标正常打开不受影响。显隐带约 400ms 淡出/淡入过渡
  （`WS_EX_LAYERED` + Alpha 渐变，保留分层样式避免闪烁），程序退出时自动恢复图标显示。
  全程使用无指针/目标进程内分配的消息，不会导致 explorer.exe 崩溃重启。

## [0.2.2] - 2026-09-12

### 新增

- **「关于」页面**：导航栏底部新增「关于」入口，集中展示应用图标、版本号、运行环境（.NET 版本 / 操作系统 / 架构）、项目与下载链接、MIT 许可证声明，以及对 wuyan1337/yySync、SteamKit2、WPF-UI 等开源项目的致谢。

## [0.2.1] - 2026-09-12

### 修复

- Release 改为**真正的单文件**：开启 `IncludeNativeLibrariesForSelfExtract`，把 WPF 原生 DLL
  （wpfgfx_cor3.dll 等）内嵌进 exe 并在首次运行时自动解压。发布产物不再出现 5 个伴随 DLL，
  下载单个 `MuSync.exe` 即可运行。

## [0.2.0] - 2026-09-12

### 变更

- **界面层从 WinForms 全面迁移到 WPF + WPF-UI 4.3.0**（Windows 11 Fluent 风格）：
  - `FluentWindow` + Mica 云母背景，深浅色自动跟随系统主题（`SystemThemeWatcher`）。
  - 左侧 `NavigationView` 三页导航：主界面 / 播放器 / 设置，页面缓存保留切换状态。
  - 主界面保留三张播放器卡片（封面、歌名、歌手、专辑、实时进度条），双击歌名打开歌曲页。
  - 新增「播放器」监控页：各播放器运行状态、错误码、当前歌曲与 Steam 会话详情。
  - 设置页改为 Win11 设置风格的圆角卡片（自定义 `SettingsCard` 样式）+ `ToggleSwitch`，保留确定/取消/应用交互，改动实时预览。
  - Steam 登录与手机令牌/邮箱验证码窗口改为 WPF 对话框（替代 WinForms 窗口与 `InputBox`）。
  - 后台逻辑（`RpcManager`、`SteamStatusManager`、`SteamSessionManager`、`ImageCacheManager` 等）全部复用不改；仅托盘状态回调入口改走 `TrayIconService`。
  - 删除 `Program.cs`、`MainForm.cs`、`SettingsForm.cs`、`SteamLoginForm.cs`，启动入口改为 `App.xaml`。

### 新增

- **自定义 Steam 在线签名**：没有播放音乐或歌曲暂停时，可在 Steam「正在玩」状态位显示自定义签名
  （设置 → Steam 显示设置 → 空闲/暂停时显示签名）。恢复播放自动切回歌曲信息（暂停期间不再推送
  `歌曲 (Paused)`）；正在玩真实 Steam 游戏、手动暂停同步或退出程序时自动隐藏。签名自动去除首尾空白并
  按 UTF-8 63 字节上限截断，设置窗口提供实时预览。
- **汽水音乐支持（替代洛雪音乐）**：通过 Windows 系统媒体会话（SMTC /
  GlobalSystemMediaTransportControls）读取歌名、歌手、播放状态、时间线与封面，无需任何启动参数或特殊设置。
- **挂游戏时长**：新增「挂时长」页，通过 LicenseList + PICS 读取 Steam 库存并列表勾选
  （按名称/AppID 搜索，最多同时 30 个，适用时长累计与集换式卡牌掉落）。音乐状态推送不中断，
  音乐条目始终位于 games_played 首位；挂时长选择持久化，重启登录后自动恢复；
  自己挂的游戏不会误触发「真实游戏时暂停音乐同步」。

### 修复

- **移除歌曲标题双击跳转浏览器**：主界面歌曲名（含「播放器未运行」状态）不再响应点击打开歌曲链接，
  同时去掉手型光标与提示。
- **界面频繁无响应**：封面图的文件 IO（`File.ReadAllBytes`）与 GDI+ 解码（`Image.FromStream`）
  原先在 UI 线程同步执行，单张大图耗时 50–200ms，连续切歌可造成数秒卡死。
  现已全部移至后台线程（`Task.Run`），内存缓存命中路径保持直接返回；Steam 在线状态推送不受影响。

### 优化

- Steam 会话建立（首次登录 / 手动重登 / 断线重连成功）后强制重推一次当前状态，
  避免空闲签名等无外部状态变化的场景在登录后不生效。

## [0.1.0] - 2026-09-07

首个公开发布版本。MuSync 是 [wuyan1337/yySync](https://github.com/wuyan1337/yySync) 的
「半新写」重构版：播放器内存逆向实现沿用原项目（MIT，见 [THIRD-PARTY-NOTICES](THIRD-PARTY-NOTICES.md)），
以下为相对 yySync 的全部改进。

### 修复

- **断线重连后不再自动重新登录的问题**：断开后仅重连不重登，同步静默失效直至重启。
  现在指数退避重连（5s → 60s），连接恢复后自动用保存的令牌重新登录。
- **启动时连接失败永不重试**：新增 20 秒 watchdog，首次连接不成功自动转入退避重连。
- **令牌重登途中断线会误删有效令牌**：现在断线保留令牌，重连成功后自动重试。
- **错误状态映射混乱**：
  - `DllNotFound`（播放器启动竞态）误报「需要管理员运行」→ 5 秒宽限期后提示「播放器组件未加载」；
  - 特征码失效（`EntryPointNotFound`）被吞成无错误、界面提示永远不会出现 → 正确提示「版本不支持/特征码失效」；
  - `OpenProcess` 拒绝访问无法识别 → 明确提示「需要管理员运行」。
- **多播放器状态冲突**：关闭任一播放器会无条件清空 Steam 状态；暂停中的播放器会霸占进度刷新。
  改为活跃源仲裁：正在播放的优先，关闭自动切换，不再误清状态。
- **封面图生命周期隐患**：切歌瞬间旧封面可能被缓存淘汰后仍被界面引用。
- **配置读取失败会直接覆盖丢失**：损坏时先备份 `config.json.bak`。
- **登录窗口「记住我」复选框无实际作用**：现在不勾选则不落盘令牌与 Guard 数据。

### 新增

- **真实游戏自动暂停**：检测到 Steam 客户端正在玩真实游戏时自动清除并暂停音乐状态，
  退出游戏自动恢复（设置中可开关，默认开启）。
- **托盘状态与手动控制**：悬停提示与右键菜单实时显示播放/暂停状态（约 5 秒刷新）；
  新增「暂停同步」手动开关。
- **主界面 Steam 会话状态标签**：未启用 / 连接中 / 未登录 / 游戏中暂停 / 同步中。
- **双击歌曲标题打开歌曲页**（浏览器）。
- **设置窗口「重新登录」入口**：令牌失效后无需重启程序。
- **Release 文件日志**：关键事件（连接/登录/重连/错误）写入
  `%LocalAppData%\MuSync\logs\`，按天滚动；稳定错误仅在首次发生时记录避免刷屏。
- **token 使用 Windows DPAPI 加密存储**（带前缀，兼容旧明文配置自动迁移）。
- **xUnit 单元测试**（14 个）：UTF-8 截断边界、状态格式化、优先级策略、DPAPI 往返等。
- **GitHub Actions CI**：构建 + 测试 + 单文件 Release 产物（`MuSync-win-x64`）。

### 优化

- LX Music HTTP 轮询从每秒约 4 次降为 1 次，请求失败沿用缓存避免误清状态。
- 无播放器运行时轮询从 233ms 退避至 1.2s，托盘驻留时停止主窗口 UI 刷新。
- 启动不再阻塞：先显示界面，后台自动登录，失败才弹登录窗口。
- 删除生产环境 `GC.Collect` 内存卫士全家桶（强制 GC、内存快照、反射计数）。
- 登录窗口中文化。
- 性能监控面板每 2 秒自动刷新，信息扩充（虚拟内存 / GC / 分类缓存计数）。

### 工程

- 项目更名 MuSync，清理 csproj 上游残留元数据与无用依赖（Newtonsoft.Json 等）。
- 解决方案拆分（主程序 + `tests/MuSync.Tests`），新增 `InternalsVisibleTo`。
- 完整 README、LICENSE（MIT）、THIRD-PARTY-NOTICES、Changelog。
